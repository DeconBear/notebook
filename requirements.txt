# ============================================
# notebook 统一依赖
# 覆盖所有25个章节的代码实操
# ============================================

# 核心计算库
numpy>=1.24.0
scipy>=1.10.0
matplotlib>=3.7.0
seaborn>=0.12.0

# 深度学习框架
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# NLP / Transformer 生态
transformers>=4.36.0
tokenizers>=0.15.0
datasets>=2.16.0
sentencepiece>=0.1.99
tiktoken>=0.5.0

# 模型微调
peft>=0.7.0
trl>=0.9.0
accelerate>=0.25.0
bitsandbytes>=0.41.0

# 推理与部署
vllm>=0.3.0
gguf>=0.6.0

# RAG / 向量检索
faiss-cpu>=1.7.4
chromadb>=0.4.0
langchain>=0.1.0

# 经典机器学习
scikit-learn>=1.3.0

# 图像处理
opencv-python>=4.8.0
Pillow>=10.0.0

# 工具
tqdm>=4.65.0
rich>=13.0.0
einops>=0.7.0

# 评估
evaluate>=0.4.0
nltk>=3.8.0

# Jupyter（本地实验）
jupyter>=1.0.0
ipywidgets>=8.0.0

# 音频（可选，多模态章节）
librosa>=0.10.0

# API 调用（可选）
openai>=1.6.0
httpx>=0.25.0

# 量子机器学习（可选，不装也不影响其它章节）
# 本源 VQNet / pyvqnet 需按其官方文档安装，不要 pip install 进本清单
# https://qcloud.originqc.com.cn/zh/programming/VQNet
